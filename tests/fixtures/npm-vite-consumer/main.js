import {createBundledWordsAnalysisEngine} from "wordswasm";

// This fixture is built by the package smoke test. It deliberately exercises
// the public convenience import that a Vite application uses at runtime.
void createBundledWordsAnalysisEngine;
