# WordsWASM native platform package

This package is installed automatically by `wordswasm-cli` for its matching
operating system and CPU architecture. Install `wordswasm-cli` instead of this
package directly.
