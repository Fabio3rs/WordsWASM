# wordswasm-cli

Install the native WordsWASM command-line interface:

```sh
npm install --global wordswasm-cli
wordswasm mālum
```

The command uses its bundled full database and `analysis-v3` by default. Pass
`--database FILE` or `--format FORMAT` to override either default.
